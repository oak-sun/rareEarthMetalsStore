spring:
  cloud:
    stream:
      kafka:
        binder:
          auto-add-partitions: true
      bindings:
        output:
          destination: creationMetalServiceDestination
          group: creationMetalServiceGroup

server:
  port: 8050